frontend:
  image: nginx:alpine
  volumes:
    - ./frontend:/usr/share/nginx/html
    - /home/ubuntu/telecom-inventory/nginx.conf:/etc/nginx/conf.d/nginx.conf  # Absolute path fix
  ports:
    - "8880:80"
  depends_on:
    - backend
  networks:
    - app-network
